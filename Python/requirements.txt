# steelg8 Python kernel dependencies
# Pinned major versions to avoid surprise API breakage.
#
# Phase 2 起不再 stdlib-only —— 需要 PDF/docx 解析和更稳的文本处理。
# 这份清单越少越好，添加新依赖需要在 PR 里说明必要性。

# ---- 必须 ----
# 目前核心服务仍走 stdlib（urllib + json + http.server）；
# 下面这些只在 RAG/项目索引路径上用到

# ---- docx 抽取 + 模板填充（Phase 2 Step 2）----
python-docx>=1.1

# ---- PDF 文本抽取（Phase 2 Step 3）----
# pypdf：纯 Python，小巧，文本型 PDF 够用
pypdf>=5.0

# ---- pptx 抽取 ----
python-pptx>=1.0

# 注：.doc（2003 老二进制）走 macOS 自带的 textutil 命令，无 pip 依赖
